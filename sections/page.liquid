{% comment %}
  Standard page. Placeholder — restyled in Phase 3.
{% endcomment %}

<div class="page-width">
  <h1 class="h2">{{ page.title }}</h1>
  <div class="rte">{{ page.content }}</div>
</div>

{% schema %}
{
  "name": "t:general.page",
  "settings": []
}
{% endschema %}
